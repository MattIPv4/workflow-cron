workflows:
  - name: PogChamp Check
    owner: MattIPv4
    repo: pogchamp
    workflow: update.yml
    ref: master
    cron:
      - '* * * * *'

  - name: Testing
    owner: MattIPv4
    repo: testing
    workflow: bad.yml
    ref: master
    cron:
      - '* * * * *'
